startCommand:
  type: stdio
  configSchema:
    type: object
    properties:
      metaAccessToken:
        type: string
        description: "Meta System User access token used directly for API calls."
      metaAppId:
        type: string
        description: "Meta App ID (Client ID) for the local OAuth flow (only needed if metaAccessToken is not provided)."
    required: []
  commandFunction: |
    (config) => {
      const env = {};
      const args = ["-m", "meta_ads_mcp"];

      if (config.metaAccessToken) {
        env.META_ACCESS_TOKEN = config.metaAccessToken;
      }

      if (config.metaAppId) {
        args.push("--app-id", config.metaAppId);
      }

      return {
        command: 'python',
        args: args,
        env: env
      };
    }
